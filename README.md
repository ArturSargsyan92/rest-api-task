# test-rest-api
Test Rest API

# Migrate
* Run script `migrate`
  - `npm run migrate`

# Seeding

* Run script `seeding` to create Admin and Moderator users and create OAuth2 Clients.
  - `npm run seeding`
